name: test_on_push.yml
on:
  push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run tests
        id: run-tests
        run: go test ./... -v